const groupAdultsByAgeRange = () => {
    const grouping = grouping.find(()=>{
        if (grouping.groupAdultsByAgeRange >= 18) {
        return true
        } else {
            return false
        }
    }) 
}
module.exports = {
    groupAdultsByAgeRange
}